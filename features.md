# Infinite Indie Sandbox — Feature Tracker

Status legend: ✅ done · 🟡 partial · 🔨 in progress · ⬜ planned

Every ✅/🟡 item is covered by the harness test suite (`npm test`) unless noted.

## Gameplay Features

### Story Campaigns (Play Sets)
| Feature | Status | Notes |
|---|---|---|
| Standalone purchasable campaign modules (themed universes) | ✅ | "The Glowlands" gallery Play Set: a 3-mission chain built purely from shipped toys (`test-campaign.js`); priced gallery entries lock behind a one-time pixel purchase on the Share screen (🔒 + price, `purchasedSet` key `playset_<file>`, buy-then-import) (`test-playset-gating.js`) |
| Action-adventure / puzzle-platformer / arena-brawler campaign types | ✅ | Arena-brawler: "Nightfall Crown" boss-arena Play Set (`test-boss-arena.js`); DUNGEON-CRAWL: "The Deepvault" cleared-room chambers (`test-dungeon.js`); PUZZLE (logic): "The Gatekeeper's Vault" OR/AND gate doors (`test-puzzle-world.js`); PUZZLE-PLATFORMER: "Skyward Steps" — jump gaps, ride the trampoline to a themed sky-ledge, grab the star, climb the ladder, dodge a moving-platform sweeper, and an AND gate opens the vault only once you've BOTH bounced AND collected the star (`test-platformer-world.js`) |
| Characters locked to campaign, usable in Sandbox | ✅ | Campaign heroes (Wick/Glowlands, Warden/Nightfall Crown): not pixel-buyable — they join the SHARED collection with their Play Set purchase; inside a campaign world (`currentWorldFile` from gallery imports) only that campaign's hero can be switched TO (the figure you arrived with keeps playing as a guest); templates/named saves clear the context and everyone plays anywhere in the Sandbox (`test-campaign-figures.js`) |
| Mission-based progression (story/side missions, collectibles) | ✅ | Quest toys chain into missions: Glowlands M1 (collect stars) physically opens the vault door, M2 (enter the pocket room) unlocks M3 (win a lap), with escalating pixel rewards and a camera-cut finale (`test-campaign.js`) |

### Combat and character systems
| Feature | Status | Notes |
|---|---|---|
| Melee combat with combo chains | ✅ | 3-hit chain; finisher deals bonus damage; swings only strike a frontal arc (player and walkers alike) (`test-combat-combo.js`) |
| Aerial juggling, blocking, dodging | ✅ | Hold `G` (pad LB) to block — negates damage from the frontal arc, shield visual; tap `C` (pad Y) to dodge-roll — i-frames + burst move (`test-blocking.js`); `R` launcher knocks enemies airborne (walkers via GravityBody velocity + stun, flyers via ballistic pop), airborne hits deal +1 and re-pop, juggle counter + toast (`test-juggling.js`) |
| Ranged combat with aimable projectiles | ✅ | Right-click fires at cursor; projectile from hand (`test-ranged-attack.js`) |
| Lock-on targeting | ✅ | `T` toggles lock-on to nearest enemy; marker + auto-aim (`test-combat-combo.js`) |
| Character-specific movesets / special attacks / super moves | ✅ | Signature `V` specials per figure (`test-specials.js`) AND per-figure MELEE MOVESETS: the combo chain itself is figure-defined — Scout's classic 1/1/3 triple, Volt's 2-hit Static Snap ending in a free bolt, Blaze's 4-hit Blazing Rush, Frost's/Warden's chilling finishers, Wick's Glow Burst launch finisher that self-juggles (launch fires before the arc, earning the airborne +1); finisher-effect radii cover melee range plus accumulated knockback (`test-movesets.js`) |
| Avatar animation (locomotion clips + idle) | ✅ | Authored walk/run/jump/strafe clips via the character controller; procedural breathing idle replaces the rig's frozen 2-frame idle range (`test-idle-animation.js`) |
| Character leveling (to 20) with skill trees | ✅ | XP from defeating enemies, level 1–20 persisted, +5 max HP/level, +1 melee dmg per 5 levels, HUD badge + XP bar (`test-progression.js`); skill tree: 1 point per level-up (derived from level), 4 skills (Vitality/Power/Trigger/Agility) with flat ranks, per-figure persistence, free respec, Esc→Skills menu (`test-skill-tree.js`) |
| Health + respawn | ✅ | HP bar, hurt cooldown, respawn at spawn point; respawn is deferred to the next frame so dying mid-combat can't crash the enemy loops (`test-death.js`) |
| Pickups: health, currency, collectibles | ✅ | `pk_health`, `pk_pixels`, `pk_star` placeable objects (`test-pickups.js`) |
| Combat power-ups (timed buffs) | ✅ | `pk_powerup` (PowerUpScript): a floating pickup granting a timed buff via `PlayMode.grantPowerUp` — 'power' DOUBLES melee/bolt damage (`powerMultiplier`), 'shield' negates all incoming damage; the timer decays each frame and the buff fades, a fresh life (respawn) clears it, and the pickup hides on collect + comes back on reset; `kind`/`duration` params, fires `collected` (`test-powerup.js`) |
| Shielder enemy (flank it) | ✅ | `en_shielder` (ShielderScript): carries a front shield and turns to face the player SLOWLY, so a quick dash-around gets you behind the guard — frontal melee/bolts/specials CLANG off it (PlayMode consults an `blocksHit` hook at every damage site), hits from behind/the sides land and fire `flanked`; `guard` arc (narrow/wide) + `toughness` params, shares the isEnemy plumbing (`test-shielder.js`) |
| Charger enemy (dash attacker) | ✅ | `en_charger` (ChargerScript): idles until you enter `range`, TELEGRAPHS with a swell wind-up, then DASHES in a straight line at your locked-in position (overshooting) before recovering — a fast dash-attacker to contrast the slow walker and stationary turret; the charge contact-damages via `damagePlayer` so it's DODGEABLE (roll the i-frames) and BLOCKABLE, shares the isEnemy damage plumbing (melee/bolts/specials kill it), and fires `charged` (`test-charger.js`) |
| Breakable blocks (destructible terrain) | ✅ | `t_breakable` (BreakableScript): a SOLID wall you smash with melee/bolts/specials — it reuses the isEnemy damage plumbing but is flagged `isBreakable` so lock-on and auto-aim ignore it; on break it goes intangible (script-owned defeat, not disposed), drops a pixel reward, and fires `broken` for wiring (a hidden passage / buried loot opens); `toughness` hits to break, wired `break` force-smashes, a play reset rebuilds it (`test-breakable.js`) |
| Sentry turrets (stationary ranged enemy) | ✅ | `en_turret` (TurretScript): a bolted-down sentry that yaw-tracks the player and fires enemy projectiles on a `range`/`cadence` (the same shots the boss uses — wall-blocked, dodgeable, blockable); attackable like any enemy but DEFEAT hides it resettably (not disposed), drops loot XP, and fires a `defeated` output for wiring; a play reset re-arms it (`test-turret.js`) |
| Boss fights (multi-phase, arena choreography) | ✅ | `en_boss`: 30 HP in three phases — stomps, then ranged volleys (≤20), then enraged shockwave slams (≤10) — hunting the nearest party member on a GravityBody; shares the `isEnemy` damage plumbing (melee/specials/bolts all land); edge-fired `phase2`/`phase3`/`defeated` wiring + `reset` input; defeat pays +25 px/+20 XP and hides resettably via the script-owned defeat hook (`test-boss.js`) |

### Vehicles and traversal
| Feature | Status | Notes |
|---|---|---|
| Drivable ground vehicles (cars, bikes, tanks) + watercraft | 🟡 | `pr_kart` hover-kart (premium, 60 px): walk-up mount, momentum driving on the shared GravityBody, ARMED forward guns (`test-vehicles.js`, `test-dogfight.js`); `pr_boat` WATERCRAFT (BoatScript, `vehicleProfile.watercraft`): rides the water surface — updateDriving eases its hull to `waterTopAt` minus a draft and cancels gravity over water (reusing the floaters helper), so it sails across pools you'd otherwise have to swim; beach it on dry land (`test-boat.js`); more vehicle types planned |
| Flyable aircraft / dogfighting | 🟡 | The Sky-Wing (`pr_wing`, `vehicleProfile.canFly` on the shared seat): runway takeoff (throttle + held Space climbs with airspeed), release to glide with a capped sink rate, banks into turns, stalls below airspeed, `C` bails out (`test-aircraft.js`); `l_ring` aerial hoops fire edge-triggered `flown` for player OR ridden vehicle and wire straight into `l_race` for fly-through courses (`test-rings.js`); DOGFIGHTING: armed vehicles (`vehicleProfile.armed`) fire forward twin bolts on a 10-frame cooldown via F/LMB/pad-X — kart and Sky-Wing armed, Strider not; bolts reuse the player-bolt damage path (kill enemies, blocked by walls) so drive-by + air combat come free (`test-dogfight.js`) |
| Ridable mounts | ✅ | The Strider (`pr_mount`): walk-up saddling on the shared vehicle seat (per-vehicle profiles), slower than the kart but pivots in place, JUMPS on Space, dismounts on `C`, legs trot with speed; mount children are collision-free so the beast can't block its own jump (`test-mounts.js`) |
| Climbing / jumping / double-jump / gliding / character traversal | ✅ | Jump, double jump, hold-to-glide (`test-traversal.js`); CLIMBING: `pr_ladder` prop — hold W near it to ascend (gravity suspended, hugged to the rungs, settles at the top), S descends, walking away restores gravity; same volume-override shape as swimming, restores after respawn (`test-climbing.js`) |
| Water blocks (swim volumes) | ✅ | `t_water`: translucent intangible unpickable volumes (the ground-ray lesson inverted — pickable water would be walkable); submersion drops gravity to a gentle sink, halves move speed, held Space strokes upward capped at the COLUMN surface (stacked pools climb to the true top), everything restores exactly on exit (`test-water.js`); floating props planned |
| Grind rails, trampolines, traversal toys | ✅ | `pr_rail` grinds a wired path chain hands-free (CC suspended, `grindStart`/`grindEnd` wiring edges, respawn bails cleanly); `t_tramp` launches at configurable power through the CC's own jump (speed borrowed then restored), squashes on launch, fires `bounced` (`test-traversal-toys.js`) |

### Multiplayer
| Feature | Status | Notes |
|---|---|---|
| 2P local split-screen campaigns (4P brawler) | ✅ | Automatic co-op split-screen (`test-splitscreen.js`) PACKAGED into campaigns: a world envelope's `coop:true` flag sets `app.coopWorld` so PlayMode auto-joins player 2 on session entry, and `l_trigger` gains a `coop` param — a two-player pressure plate that fires only when the player AND a live buddy are both inside; "Twin Trials" gallery Play Set (stand-together vault gate + a cleared-gated team arena) (`test-coop-campaign.js`); 4P brawler campaigns build on the same flag |
| Up to 4P Sandbox (local + online) | 🟡 | Local 4P: three buddy slots (green/orange/violet), each on its own gamepad (or `B` to fill), all hunted via `combatTargets`, per-buddy downed cycles, party respawn, and split-screen growing to a 2×2 quadrant grid (`test-fourplayer.js`); ONLINE SPIKE landed: `NetLink` protocol (world snapshot on connect + ~10Hz transform stream + bye) over manually-signaled WebRTC DataChannels — no server, no deps; remote player renders as a lerped ghost rig; suite exercises a REAL loopback RTC pair (`test-netlink.js`); Online screen (main menu → 8): Host/Answer/Join actions trade the two codes via clipboard (async-rejection-safe, `app.lastNetCode` console fallback), and a received world auto-creates the guest's SandboxWorld and drops them into play mode (`test-net-menu.js`); LIVE EDIT STREAMING: placements and deletions commit across the wire symmetrically (fresh local ids + a remote-id map for deletes; params intact) so linked players build together; WIRING streams too — addWire/removeWire commit points send, the receiver resolves both endpoints through the remote-id map (falling back to shared snapshot ids) and a mute guard kills echoes; DROPPED links tear down cleanly (ghost + remote map), flag the Online menu for a fresh code trade (host re-ships the world on re-link), and mode switches never strand a frozen ghost; MULTIPLAYER STAR: the host's NetHub takes one link per guest (welcome assigns g1/g2/… ids), relays guest traffic to every other guest, and everyone renders per-player tinted ghosts wearing billboard NAME TAGS; the Online screen is hub-aware (guest count, one-code-per-friend wording) (`test-netlink.js`) |
| Drop-in/drop-out co-op | ✅ | Drop-in buddy: `B` or any second-pad button joins; stick/A/X drive move/jump/melee; enemies hunt the NEAREST player (flyers, walker melee, projectiles all route via `combatTargets`/`damageTarget`), buddy has 60 HP with a downed-then-revive-at-half cycle (out of targeting while down), falls auto-rescue, respawn restores it, and the camera widens to frame both players (`test-coop.js`); full P2 figure avatar planned |

### Digital figures and collection
| Feature | Status | Notes |
|---|---|---|
| Purchasable figure packs / à la carte | ✅ | Figures purchasable à la carte with pixels (`test-collection.js`); shop packs bundle figures + premium objects at a discount (Hero Pack, Winter/Neon Play Sets) — flat price grants all missing contents, ownership derived from contents (`test-figure-packs.js`) |
| Collection screen as roster (select + spawn any owned figure) | ✅ | Collection menu (main + pause): 4 figures with colorways + stat leans; selecting applies live (`test-collection.js`) |
| Per-figure progress saved to account | ✅ | Level/XP persisted per figure; switching figures swaps progression (`test-collection.js`) |
| NPC villagers (wander, talk bubbles, quest hooks) | ✅ | `pr_villager`: ambles on a leash around home (GravityBody, dt-based), stops to face the player and shows a billboard talk bubble cycling its mood's lines (friendly/mysterious/grumpy/heroic), edge-fires `talked` per fresh greet (distinct-source quest fodder), `say` input forces a timed bubble for cinematics, play reset sends it home (`test-villagers.js`) |
| Hireable companions (dialog trees, per-slot crew) | ✅ | `pr_recruit` NPCs open digit-driven DIALOG TREES (MENU_DIALOG rides the stock menu system); Fern is free, Rusty 40 px, Lumen 80 px — broke hires refuse in-conversation; the roster is a PROGRESSION key so each slot keeps its own crew (shared-collection snapshots exclude it), and PlayMode rebuilds ground-walking follower rigs whenever the roster signature changes: hire, dismiss, slot switch, world entry (`test-companions.js`) |
| Round power discs (abilities/buffs) | ✅ | 5 buyable discs (Ember +1 melee, Aegis +20 HP, Swift faster dodge, Fortune +25% pixels, Sage +25% XP), 2 equip slots, global across figures, persisted; Discs screen via Collection → 9 (`test-discs.js`) |
| Hex discs (vehicles, gadgets, skies/terrain) | ✅ | Sky/terrain theme hexes (`test-hex-discs.js`) AND gadget hexes — equippable passive perks, one active, bought once into the shared collection with the active choice per progression slot, applied on session start: Pixel Magnet (bursts rush straight in via a wide collect radius), Boost Boots (higher CC jump, baseline raised so trampoline restores keep it), Guardian Ward (absorbs the first hit each life, recharges on respawn); Discs-screen rows (`test-gadget-hex.js`) |
| Multiple discs stack/combine | ✅ | Two different equipped discs both apply (Ember + Aegis verified stacking in `test-discs.js`) |

## Sandbox Features

### Creation tools
| Feature | Status | Notes |
|---|---|---|
| Open building mode with placeable objects | ✅ | Object browser w/ runtime thumbnails, categories, arrow-key cycling |
| Magic-wand: place / move / rotate / scale / link / delete | ✅ | Cursor mode: Space grabs (one key with placement), Shift+Space opens properties, mouse TAP selects any placed object; left sidebar lists the browsed category (arrow keys + clickable rows); R/V raise, Z/C rotate, [/] scale, Delete (`test-build-ux.js`, `test-move-object.js`); wiring view: tap an object for its inspector card (ports + wires), tap a wire for its card — Delete lives inside the card (`test-build-ux.js`) |
| Terrain sculpting with block terrain, themes | ✅ | Block tiles w/ top-snap anchor + rolling default grid; THEMED terrain — sand, snow, VOLCANIC (charred basalt + ember crust), and TOXIC (alien green) — tile and 2-block sizes: per-theme procedural atlases drawn on canvas (base coat + speckle, zero image assets), one shared material per theme so blocks stay instancable, themed footstep surfaces (`test-terrain-themes.js`) |
| Multi-select (shift-click groups) | ✅ | Shift-click TOGGLES objects into a multi-selection (add + remove); Delete removes the whole group (and one U undoes it as one restore), and F duplicates the whole group in place (copies land, the selection becomes the copies) — a single-object F still grabs a copy to move; a shared `_copyInstance` backs both duplicate paths (`test-multiselect.js`) |
| Undo deletions (U) | ✅ | `U` in cursor mode restores the most recent deletion — a delete-history stack snapshots each removed object (position/rotation/scale/params/wires, deep-copied) and un-delete recreates them into the world with everything intact; a multi-object delete restores as one group, and undo on an empty history is a safe no-op (`test-undo.js`) |
| Object duplication (F) | ✅ | In cursor mode, `F` stamps a copy of the highlighted object — a fresh instance carrying the original's rotation, scale, and per-instance params (deep-copied; edits to the copy don't touch the original) but NOT its wires — grabbed and ready to place, so you can lay out rows of a configured object without re-browsing or re-tuning (`test-duplicate.js`) |
| Snap-assisted placement (vertex/rotation snapping) | ✅ | Hold Shift or pad LB (or latch with CapsLock): movement keys JUMP the moving object flush against the nearest object in that direction (dominant world axis, grid pull suspended, idempotent re-press); rotation keys MATCH the nearest same-type or similar-size piece within 12u (AABB similarity) (`test-snap.js`) |
| Keys & locked doors (inventory adventure) | ✅ | `pk_key` (KeyScript) + `pr_lock` (LockScript): collect a colored key (added to `mode.keysHeld`, a per-run Set), then walk into the matching-color locked barrier to open it — the lock is a SOLID wall until unlocked, which CONSUMES the key, goes intangible, and fires `unlocked`; a wrong-color key won't open it, a wired `unlock` force-opens without a key, and a play reset re-locks + empties the ring (`test-keylock.js`) |
| Barrel cannons (launch traversal) | ✅ | `pr_cannon` (CannonScript): step into it and it flings the player along the barrel's facing in a ballistic arc — a new `PlayMode.launchPlayer` combines the trampoline's CC-jump borrow (height) with a decaying per-frame horizontal impulse (the dodge-velocity pattern); rotate the barrel to aim, `power`/`reach` params scale height and distance, a cooldown prevents instant re-fire, fires `fired` (the barrel is intangible so it can't block its own shot; launch is a combined forward+up impulse so it works mid-air with the CC's gravity forming the arc) (`test-cannon.js`) |
| Moving hazards (sweeping blades) | ✅ | `l_sweeper` (SweeperScript): a damage volume that OSCILLATES along an axis from its home — time your crossing; it hurts on contact (AABB + `damagePlayer`, so a dodge rolls through), `axis`/`reach`/`speed`/`damage` params, fires `hurt` on a hit and `swept` each pass through centre, resets home (`test-sweeper.js`) |
| Wind / updraft zones | ✅ | `l_fan` (FanScript): an intangible volume that pushes the player in a chosen direction while inside — an UP fan lifts you up a shaft (a continuous jump-pad, no ladder), a horizontal fan blows you across a gap; moves the player via moveWithCollisions (walls still stop them), vehicles ignore it, wire `on`/`off` to toggle, resets on for the next run; AABB containment, `dir`/`strength` params (`test-fan.js`) |
| Environmental hazards (damage zones) | ✅ | `l_hazard` (HazardScript): an intangible translucent-red volume that hurts the player on a tick while they stand in it — via `damagePlayer`, so dodge i-frames roll you through and death defers-then-respawns at your last checkpoint; AABB containment (matrix-independent), `damage`/`interval` params, fires `hurt` only when damage actually lands (`test-hazard.js`) |
| Teleporter pads | ✅ | `l_teleport` (TeleportScript): wire one pad's `link` output to another pad's `here` input and stepping on the first whisks the player to the partner (lifted a little); wire it both ways for a two-way portal; a shared post-teleport cooldown (`mode._teleportCool`) stops the exit pad ping-ponging you back; vehicle riders aren't teleported; fires `used` for wiring (`test-teleport.js`) |
| Checkpoints / respawn flags | ✅ | `l_checkpoint` (CheckpointScript): touch it in play to make it your respawn point — die after and you return HERE, not the world spawn; only one active at a time (a new one takes over and lowers the rest), the flag RAISES up its pole when active (per-instance local offset, instancing-safe), fires `reached` once per activation, and respawn persistence survives death by design (a play reset keeps it) (`test-checkpoint.js`) |
| Logic toys: gates (AND/OR/NOT) | ✅ | `l_gate` (GateScript): a Boolean combinator for puzzle wiring — listens to `on`/`off` from any number of wired sources (tracked by the `from` instance), edge-fires its own `on`/`off` when the condition flips; ALL = ≥`need` sources active (explicit count so a 2-plate door is real), ANY = ≥1 active, NOT = a source signalled but none active; play reset clears it (`test-gate.js`) |
| Logic toys: triggers | ✅ | `l_trigger` volumes fire enter/exit events; a `coop` param makes a two-player pressure plate that fires only when two party members are inside (`test-wiring.js`, `test-coop-campaign.js`) |
| Logic toys: spawners | ✅ | `l_spawner` w/ enemy type / frequency / limit params, plus a `wave` quota that fires a `cleared` output when the room empties — the dungeon-crawl primitive (`test-spawner.js`, `test-dungeon.js`) |
| Logic toys: counters | ✅ | `l_counter` — increment/decrement/reset inputs, reached/changed outputs (`test-logic-toys.js`) |
| Logic toys: timers | ✅ | `l_timer` — start/stop/reset inputs, tick output, interval/repeat params (`test-logic-toys.js`) |
| Logic toys: scoreboards | ✅ | `l_scoreboard` — add/subtract/reset inputs, HUD score display, edge-triggered `reached` output (`test-progression.js`) |
| Logic toys: cameras | ✅ | `l_camera` — wires cut the view to it for N seconds (player-tracking or fixed shot), `started`/`finished` outputs chain cinematics (`test-camera-toy.js`) |
| Event wiring between logic toys | ✅ | Overhead 3D wiring view; wires persist in saves (`test-wiring.js`) |
| Interiors (rooms, doors, decoration) | ✅ | INTERIOR category room kit (`in_wall`, `in_wall_door`, `in_wall_window`, `in_floor` — walls block shots, openings pass), wirable sliding door (`pr_door`), furniture (`d_table/chair/lamp/rug`), multi-prim manifest support (`test-interiors.js`); pocket-interior cell door (`pr_door_cell`: walk in → teleport to its room, exit pad returns, outdoor enemies freeze, entered/exited wiring events, dollhouse camera); the room is REAL world objects furnished once from the theme template at the door's cell origin — editable in build mode, serialized inside the owning world, never refurnished over player edits (`test-interior-cells.js`) |
| Path/track creation (races, patrols, moving platforms) | ✅ | `l_pathnode` waypoints chain via wires; `pr_platform_moving` follows the chain dt-based; `en_blob` patrols a wired chain (`test-paths.js`); `l_race` packages start gate + distinct-checkpoint tracking + finish line with a HUD stopwatch, `finished`/`record` outputs; best times persist per save slot via `params.bestTime` (`test-races.js`) |
| Music/SFX toys (synthesized, wired) | ✅ | `l_chime`: a `play` input synthesizes its pattern via WebAudio oscillators (zero asset files) — jingle/alarm/gong/powerup with volume param — and fires `played` for chaining; App gains a lazy audio layer (`audio()`/`playTones`, seq-marked for suspended-context tests) (`test-sound.js`) |
| Day/night cycle (wired sun, mood lighting) | ✅ | `l_sun`: play-mode time cycles the hemispheric light (bright noon → dim night, warm dawn/dusk tint) and darkens the sky from the CAPTURED baseline (hex themes compose); edge-fired `dawn`/`noon`/`dusk`/`midnight` wiring + `start`/`stop` inputs; build mode and removal restore exact daylight; play reset returns to dawn (`test-daynight.js`) |
| Photo mode (freeze + free camera + capture) | ✅ | `P` in play mode freezes the whole world (update short-circuit) and hides the HUD; WASD/R/F dolly the free camera (Shift speeds it) with mouse orbit live; `Enter` captures a 1280×720 PNG via `CreateScreenshotUsingRenderTarget` (downloads, `app.lastPhotoData` for tests); `P` resumes, Esc-teardown restores the HUD (`test-photo.js`) |
| AI builder assistants | ✅ | Local offline generative builder (`BuildAssistant`, no LLM/network — true to the static-file rule): a deterministic intent parser reads a plain-language request for a known RECIPE + size/count/theme modifiers and places REAL wired world objects around the player — walled arena (floor+walls+trigger→spawner), star trail (count → counter→scoreboard), patrol (looped path chain + wired blob), tower/wall/platform, ring course (rings→race); `K` in build mode prompts (`test-assistant.js`) |
| Named world saves + progression slots | ✅ | Worlds save under NAMES (pause → Save/Load World, prompt with a test hook; overwrite rows; legacy numbered saves migrate to "Slot N"); 3 numbered PROGRESSION slots hold pixels/levels/skills/companions as snapshots (`iis_slotdata_N` — live keys always mirror the active slot) while ownership keys are never snapshotted, so the collection is shared by construction; slot picker on the main menu, world save/load mid-session (`test-named-saves.js`) |
| Templates / starter worlds | ✅ | New Game picker: Rolling Hills / Flat Plane / Arena / Floating Islands (`test-progression.js`) / Sandbox Hub challenge park (`test-hub.js`) |
| Unlockable toys through play | ✅ | Pixels earned from enemies buy locked objects in the shop (`test-shop-gating.js`) |

### The Sandbox Hub
| Feature | Status | Notes |
|---|---|---|
| Central hub world with zones/quests/challenges | ✅ | New Game template 5 "Sandbox Hub": central plaza + 4 pre-wired zones — Combat Yard (trigger→spawner+camera cut), Star Climb (4 stars→counter→scoreboard), The Crossing (ping-pong ferry + patrolling guard), Homestead (room kit + sliding door + cell door) (`test-hub.js`); `l_quest` quest toy — distinct wired steps, once-per-run completion with a pixel reward, hub ships a 3-step "Tour the Park" quest (`test-quests.js`) |

### Sidekicks
| Feature | Status | Notes |
|---|---|---|
| Collectible sidekicks that follow/level/feed/equip | ✅ | 3 adoptable sidekicks, hover-follow, half-share XP leveling + feeding, +2 max HP aura per level (`test-sidekicks.js`); gear crafted from farmed food (Top Hat +2 HP, Bell rounds XP share up, Cape +5 meal XP) worn per-sidekick in hat/trinket slots with visible accessories, managed on the Sidekick Care screen (`test-sidekick-gear.js`) |
| Crop farming for sidekick food | ✅ | `pr_plot` grows glowberries dt-based (growTime/autoReplant params, per-instance crop material, green→gold stages); walking over a ripe plot harvests +2 food and fires a wirable `harvested`; food is the premium sidekick meal (1 → 15 XP, pixel fallback), persisted (`test-farming.js`) |

### Sandbox Expansion Games
| Feature | Status | Notes |
|---|---|---|
| Kart racing expansion | ✅ | Kart + triggers + `l_race` compose into lap racing (the VEHICLE trips triggers; Glow Circuit's gate wires finish-then-start for a wire-order lap timer) (`test-kart-racing.js`); `pr_kart_ghost` AI rivals lap wired path chains — translucent, intangible, `lapped` output, shipped on Glow Circuit's 4-node racing line (`test-ghost-kart.js`) |
| Water floating props | ✅ | `pr_barrel` / `pr_crate` (BuoyScript): buoyant props ease onto the water surface and ride mostly submerged with a gentle sine bob + slow spin, instead of sinking; on dry land they're left alone; a play reset returns a drifted floater home; the water surface lookup (`waterTopAt`/`waterSurfaceAt`) is shared with swimming and climbs stacked pools (`test-floaters.js`) |
| Treasure chests / loot rewards | ✅ | `pr_chest` (ChestScript): walk up (auto param) or wire the `open` input to pop the hinged lid and spill a pixel reward as a homing burst (credited through the enemy-loot pixel loop; big rewards cap the burst at 30 and credit the overflow directly); fires `opened` for wiring, one-shot per run, a play reset closes + re-arms it (`test-chest.js`) |
| Co-op dungeon-crawl expansion | ✅ | New "clear the room" primitive: the spawner takes a `wave` quota and fires a one-shot `cleared` output when its whole wave has spawned AND all its spawns are defeated (re-arms on play reset); "The Deepvault" premium Play Set (250 px) chains three chambers — gate arms wave 1 → cleared opens door 1 + steps quest, threshold arms wave 2 → cleared opens door 2, boss defeated opens the loot vault + scoreboard — with camera cuts, a healing pickup, and a recruit (`test-dungeon.js`) |

### Community features
| Feature | Status | Notes |
|---|---|---|
| Saving multiple Sandbox worlds locally | ✅ | 9 save slots, transforms/params/wires persist (`test-save-load.js`) |
| Upload + share creations online | 🟡 | Serverless sharing via world files: main menu → Share Worlds exports the current world as a versioned `.json` (same snapshot as save slots) and imports validate format/version, rejecting bad files without touching the world; unknown object types skip gracefully (`test-share.js`); online gallery planned |
| Browse/rate/download community worlds | 🟡 | Gallery browsing on the Share screen: `assets/worlds/index.json` catalog fetched over HTTP, one-digit import into build mode, plus `importWorldFromUrl` for arbitrary links; ships a full gallery — Starter Parkour, Tiny Arena, the four campaign worlds (Nightfall Crown, Deepvault, Gatekeeper's Vault, Skyward Steps), the boat-voyage "Tidewater Run", and the "Aether Ruins" adventure, the "Locked Depths" key-and-lock dungeon, and the "Skybreak Run" cannon-jump course, and "The Gauntlet" wave-survival arena (spawner waves + charging brutes + a sentry turret against the dodge/block kit, wave-clear opens the vault) (`test-gallery.js`, `test-water-world.js`, `test-adventure-world.js`, `test-dungeon-world.js`, `test-launch-world.js`, `test-arena-world.js`); LOCAL FAVOURITES: star gallery worlds (persisted in `iis_fav_worlds`); a favourite-mode toggle makes a gallery pick star/unstar instead of loading, and `orderedGallery` floats favourites to the top after the featured pick (`test-favorites.js`); remote gallery + server ratings planned |
| Featured/curated rotations | ✅ | The gallery index's `featured` list rotates one pick per day (deterministic `day % n`, injectable for tests); the Share screen sorts it first with a ★ FEATURED badge and digit 3 always imports today's pick (`test-featured.js`) |

## Engine / platform underpinnings (not on the public list)
| Feature | Status | Notes |
|---|---|---|
| Linux/CI test harness (headless, screenshots, frame sampling) | ✅ | `test/harness.js`, 16+ suites, GitHub Actions CI |
| Gamepad bindings (attack actions) | ✅ | Declarative `PAD_MAP` behind one `handlePadButton` entry point: A hold = jump/glide, X/LT melee, B dodge, Y special, RB/RT ranged, LB hold block, right-stick click lock-on; left stick drives movement through the controller's real key handlers (8-way + hysteresis), right stick orbits the camera; `app.testPad` harness hook (`test-gamepad.js`) |
| In-game economy (pixels) | ✅ | Earn from enemies, spend in shop, persists |
| Sound effects (recorded pack + synth fallback) | ✅ | `SoundManager` (Web Audio): every event maps to WAVs from `assets/sounds/` (lazy preload after first input, random take + pitch variation, per-event gain); per-surface footsteps (grass/dirt/sand/snow/wood/stone/carpet/metal — grass blocks resolve by face), jump/double-jump/land/glide, full combat + survival set, pickups/pixels/level-up/purchase, UI/build/wiring clicks; synth recipes cover unloaded/failed samples; `M` = persistent mute (`test-sfx.js`) |
| Real image textures (CC0 pack) | ✅ | `App.PACK_TEX` maps texture ids to the Screaming Brain 128×128 pack (`assets/textures/1`): warm wood (doors/furniture), weathered `planks` (interior floors), red brick (walls/cell doors), cream marble tile (blobs/pickups), chunky lawn grass baked 2×2 into the terrain atlas, sickly moss on the toxic theme's crust; procedural cloud/noise/road/starfield stay (no pack match); Roofs + 100-odd unused variants available for future objects (`test-texture-category.js`) |

## Next up (suggested order)
